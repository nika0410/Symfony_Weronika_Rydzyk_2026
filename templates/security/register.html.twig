{% extends 'base.html.twig' %}

{% block title %}{{ 'title.register'|trans }}{% endblock %}

{% block body %}
    <h1>{{ 'title.register'|trans }}</h1>
    {{ form_start(form) }}
    {{ form_widget(form) }}
    <button class="btn btn-primary" type="submit">{{ 'action.register'|trans }}</button>
    {{ form_end(form) }}
{% endblock %}
